<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                    xmlns:converters="clr-namespace:Spectralyzer.App.Host.Converters"
                    xmlns:helpers="clr-namespace:Spectralyzer.App.Host.Helpers"
                    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                    xmlns:viewModels="clr-namespace:Spectralyzer.App.Host.Features.TrafficAnalyzer.ViewModels"
                    xmlns:wpf="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">

    <DataTemplate DataType="{x:Type viewModels:TrafficAnalyzerItem}">
        <DockPanel LastChildFill="True">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                <ui:Button MinWidth="100"
                           Margin="5"
                           helpers:VisibilityHelper.IsCollapsed="{Binding IsCapturingTraffic}"
                           Appearance="Primary"
                           Command="{Binding StartCaptureCommand}"
                           Content="Start capture" />
                <ui:Button MinWidth="100"
                           Margin="5"
                           helpers:VisibilityHelper.IsVisible="{Binding IsCapturingTraffic}"
                           Appearance="Primary"
                           Command="{Binding StopCaptureCommand}"
                           Content="Stop capture" />
                <ui:TextBlock Margin="5"
                              VerticalAlignment="Center"
                              Appearance="Secondary"
                              FontSize="14"
                              Text="Port:" />
                <ui:NumberBox MinWidth="150"
                              Margin="5"
                              IsEnabled="{Binding IsCapturingTraffic, Converter={x:Static converters:InvertedBoolConverter.Instance}}"
                              Maximum="65535"
                              Minimum="1"
                              Value="{Binding Port}" />
                <CheckBox Margin="5"
                          VerticalAlignment="Center"
                          Content="Decrypt HTTPS traffic"
                          IsChecked="{Binding DecryptSsl}"
                          IsEnabled="{Binding IsCapturingTraffic, Converter={x:Static converters:InvertedBoolConverter.Instance}}" />
                <ui:Button MinWidth="100"
                           Margin="5"
                           Command="{Binding ClearSessionsCommand}"
                           Content="Clear"
                           IsEnabled="{Binding HasSessions}" />
            </StackPanel>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="80*" MinHeight="200" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="20*" MinHeight="100" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="35*" MinWidth="200" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="65*" MinWidth="400" />
                    </Grid.ColumnDefinitions>
                    <DataGrid Grid.Column="0"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              AutoGenerateColumns="False"
                              CanUserSortColumns="True"
                              EnableRowVirtualization="True"
                              HeadersVisibility="Column"
                              ItemsSource="{Binding WebSessions}"
                              SelectedItem="{Binding SelectedWebSession}">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn MinWidth="50"
                                                    Header="#"
                                                    IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox VerticalAlignment="Top"
                                                 VerticalContentAlignment="Center"
                                                 IsReadOnly="True"
                                                 Text="{Binding Index, Mode=OneWay}"
                                                 TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn MinWidth="100"
                                                    Header="Process"
                                                    IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox VerticalAlignment="Top"
                                                 VerticalContentAlignment="Center"
                                                 IsReadOnly="True"
                                                 Text="{Binding Process, Mode=OneWay}"
                                                 TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn MinWidth="100"
                                                    Header="Host"
                                                    IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox VerticalAlignment="Top"
                                                 VerticalContentAlignment="Center"
                                                 IsReadOnly="True"
                                                 Text="{Binding RequestMessage.RequestUri.Host, Mode=OneWay}"
                                                 TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn MinWidth="200"
                                                    Header="URL"
                                                    IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox VerticalAlignment="Top"
                                                 VerticalContentAlignment="Center"
                                                 IsReadOnly="True"
                                                 Text="{Binding RequestMessage.RequestUri.PathAndQuery, Mode=OneWay}"
                                                 TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn MinWidth="100"
                                                    Header="Status"
                                                    IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox VerticalAlignment="Top"
                                                 VerticalContentAlignment="Center"
                                                 IsReadOnly="True"
                                                 Text="{Binding ResponseMessage.StatusDescription, Mode=OneWay}"
                                                 TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <GridSplitter Grid.Column="1"
                                  Width="5"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Stretch" />
                    <Grid Grid.Column="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" MinWidth="200" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" MinWidth="200" />
                        </Grid.ColumnDefinitions>
                        <TabControl Grid.Column="0">
                            <TabItem Header="HTTP">
                                <wpf:WebView2 x:Name="WebView2"
                                              Margin="0"
                                              HorizontalAlignment="Stretch"
                                              VerticalAlignment="Stretch">
                                    <b:Interaction.Triggers>
                                        <b:EventTrigger EventName="Loaded">
                                            <b:InvokeCommandAction Command="{Binding SelectedWebSession.RequestMessage.InitializeCommand}" CommandParameter="{Binding ElementName=WebView2}" />
                                        </b:EventTrigger>
                                        <b:EventTrigger EventName="NavigationCompleted">
                                            <b:InvokeCommandAction Command="{Binding SelectedWebSession.RequestMessage.InitializeEditorCommand}" CommandParameter="{Binding ElementName=WebView2}" />
                                        </b:EventTrigger>
                                    </b:Interaction.Triggers>
                                </wpf:WebView2>
                            </TabItem>
                            <TabItem Header="Preview">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="50*" MinHeight="100" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="50*" MinHeight="100" />
                                    </Grid.RowDefinitions>
                                    <DataGrid Grid.Row="0"
                                              HorizontalAlignment="Stretch"
                                              VerticalAlignment="Stretch"
                                              AutoGenerateColumns="False"
                                              CanUserSortColumns="True"
                                              EnableRowVirtualization="True"
                                              HeadersVisibility="Column"
                                              ItemsSource="{Binding SelectedWebSession.RequestMessage.Headers}">
                                        <DataGrid.Columns>
                                            <DataGridTemplateColumn Width="*"
                                                                    MinWidth="100"
                                                                    Header="Key"
                                                                    IsReadOnly="True">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBox VerticalAlignment="Top"
                                                                 VerticalContentAlignment="Center"
                                                                 IsReadOnly="True"
                                                                 Text="{Binding Key, Mode=OneWay}"
                                                                 TextWrapping="Wrap" />
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTemplateColumn Width="*"
                                                                    MinWidth="100"
                                                                    Header="Value"
                                                                    IsReadOnly="True">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <ItemsControl ItemsSource="{Binding Values}">
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <TextBox VerticalAlignment="Top"
                                                                             VerticalContentAlignment="Center"
                                                                             IsReadOnly="True"
                                                                             Text="{Binding ., Mode=OneWay}"
                                                                             TextWrapping="Wrap" />
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                    <GridSplitter Grid.Row="1"
                                                  Height="5"
                                                  HorizontalAlignment="Stretch"
                                                  VerticalAlignment="Center" />
                                    <Grid Grid.Row="2">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                        <ComboBox Grid.Row="0"
                                                  Margin="5"
                                                  HorizontalAlignment="Left"
                                                  ItemsSource="{Binding SelectedWebSession.RequestMessage.Formats}"
                                                  SelectedItem="{Binding SelectedWebSession.RequestMessage.SelectedFormat}" />
                                        <wpf:WebView2 x:Name="WebView21"
                                                      Grid.Row="1"
                                                      Margin="0"
                                                      HorizontalAlignment="Stretch"
                                                      VerticalAlignment="Stretch">
                                            <b:Interaction.Triggers>
                                                <b:EventTrigger EventName="Loaded">
                                                    <b:InvokeCommandAction Command="{Binding SelectedWebSession.RequestMessage.InitializeCommand}" CommandParameter="{Binding ElementName=WebView21}" />
                                                </b:EventTrigger>
                                                <b:EventTrigger EventName="NavigationCompleted">
                                                    <b:InvokeCommandAction Command="{Binding SelectedWebSession.RequestMessage.InitializeEditorCommand}" CommandParameter="{Binding ElementName=WebView21}" />
                                                </b:EventTrigger>
                                            </b:Interaction.Triggers>
                                        </wpf:WebView2>
                                    </Grid>
                                </Grid>
                            </TabItem>
                        </TabControl>
                        <GridSplitter Grid.Column="1"
                                      Width="5"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Stretch" />
                        <TabControl Grid.Column="2">
                            <TabItem Header="HTTP">
                                <wpf:WebView2 x:Name="WebView22"
                                              Margin="0"
                                              HorizontalAlignment="Stretch"
                                              VerticalAlignment="Stretch">
                                    <b:Interaction.Triggers>
                                        <b:EventTrigger EventName="Loaded">
                                            <b:InvokeCommandAction Command="{Binding SelectedWebSession.ResponseMessage.InitializeCommand}" CommandParameter="{Binding ElementName=WebView22}" />
                                        </b:EventTrigger>
                                        <b:EventTrigger EventName="NavigationCompleted">
                                            <b:InvokeCommandAction Command="{Binding SelectedWebSession.ResponseMessage.InitializeEditorCommand}" CommandParameter="{Binding ElementName=WebView22}" />
                                        </b:EventTrigger>
                                    </b:Interaction.Triggers>
                                </wpf:WebView2>
                            </TabItem>
                            <TabItem Header="Preview">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="50*" MinHeight="100" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="50*" MinHeight="100" />
                                    </Grid.RowDefinitions>
                                    <DataGrid Grid.Row="0"
                                              HorizontalAlignment="Stretch"
                                              VerticalAlignment="Stretch"
                                              AutoGenerateColumns="False"
                                              CanUserSortColumns="True"
                                              EnableRowVirtualization="True"
                                              HeadersVisibility="Column"
                                              ItemsSource="{Binding SelectedWebSession.ResponseMessage.Headers}">
                                        <DataGrid.Columns>
                                            <DataGridTemplateColumn Width="*"
                                                                    MinWidth="100"
                                                                    Header="Key"
                                                                    IsReadOnly="True">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <TextBox VerticalAlignment="Top"
                                                                 VerticalContentAlignment="Center"
                                                                 IsReadOnly="True"
                                                                 Text="{Binding Key, Mode=OneWay}"
                                                                 TextWrapping="Wrap" />
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTemplateColumn Width="*"
                                                                    MinWidth="100"
                                                                    Header="Value"
                                                                    IsReadOnly="True">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <ItemsControl ItemsSource="{Binding Values}">
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <TextBox VerticalAlignment="Top"
                                                                             VerticalContentAlignment="Center"
                                                                             IsReadOnly="True"
                                                                             Text="{Binding ., Mode=OneWay}"
                                                                             TextWrapping="Wrap" />
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                    <GridSplitter Grid.Row="1"
                                                  Height="5"
                                                  HorizontalAlignment="Stretch"
                                                  VerticalAlignment="Center" />
                                    <Grid Grid.Row="2">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                        <ComboBox Grid.Row="0"
                                                  Margin="5"
                                                  HorizontalAlignment="Left"
                                                  ItemsSource="{Binding SelectedWebSession.ResponseMessage.Formats}"
                                                  SelectedItem="{Binding SelectedWebSession.ResponseMessage.SelectedFormat}" />
                                        <wpf:WebView2 x:Name="WebView23"
                                                      Grid.Row="1"
                                                      Margin="0"
                                                      HorizontalAlignment="Stretch"
                                                      VerticalAlignment="Stretch">
                                            <b:Interaction.Triggers>
                                                <b:EventTrigger EventName="Loaded">
                                                    <b:InvokeCommandAction Command="{Binding SelectedWebSession.ResponseMessage.InitializeCommand}" CommandParameter="{Binding ElementName=WebView23}" />
                                                </b:EventTrigger>
                                                <b:EventTrigger EventName="NavigationCompleted">
                                                    <b:InvokeCommandAction Command="{Binding SelectedWebSession.ResponseMessage.InitializeEditorCommand}" CommandParameter="{Binding ElementName=WebView23}" />
                                                </b:EventTrigger>
                                            </b:Interaction.Triggers>
                                        </wpf:WebView2>
                                    </Grid>
                                </Grid>
                            </TabItem>
                        </TabControl>
                    </Grid>
                </Grid>
                <GridSplitter Grid.Row="1"
                              Height="5"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Center" />
                <Grid Grid.Row="2">
                    <DataGrid HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              AutoGenerateColumns="False"
                              EnableRowVirtualization="True"
                              HeadersVisibility="Column"
                              ItemsSource="{Binding Errors}">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="*"
                                                    MinWidth="100"
                                                    Header="Exception"
                                                    IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox VerticalAlignment="Top"
                                                 VerticalContentAlignment="Center"
                                                 IsReadOnly="True"
                                                 Text="{Binding ., Mode=OneWay}"
                                                 TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Grid>
        </DockPanel>
    </DataTemplate>

</ResourceDictionary>